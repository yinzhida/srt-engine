import getLogger from '@/js/utils/logger';
// import

class SrtEngine {
  constructor () {
    this.content = null;
    this.logger = getLogger('SrtEngine');
    this.url = '';
    this.dataJSON = null;
  }

  load (url) {
  }

  compile (content) {
  }
}

export default SrtEngine;
